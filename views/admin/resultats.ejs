<%- include('_head', { page: 'resultats' }) %>

<div class="card">
  <div class="admin-card-entete">
    <div class="carte-titre">
      <div class="carte-eyebrow">Scrutin<% if (electionAffichee) { %> · <%= electionAffichee.titre %><% } %></div>
      <h2>Résultats <% if (tourAffiche) { %>· Tour <%= tourAffiche.numero %><% } %></h2>
    </div>
    <a href="/admin/pv" class="bouton bouton-principal bouton-petit" title="Télécharger le procès-verbal en PDF">📄 PV (PDF)</a>
  </div>

  <% if (tours.length > 1) { %>
    <div class="selecteur-tours">
      <% tours.forEach(function(t) { %>
        <a href="/admin/resultats?election=<%= electionAffichee.id %>&tour=<%= t.numero %>" class="tour-pill <%= tourAffiche && tourAffiche.numero === t.numero ? 'actif' : '' %>">Tour <%= t.numero %></a>
      <% }); %>
    </div>
  <% } %>

  <ul class="stats-liste">
    <li>Votes enregistrés : <strong><%= totalVotants %></strong></li>
    <li>Électeurs inscrits : <strong><%= totalElecteurs %></strong></li>
    <li>Taux de participation : <strong><%= tauxParticipation %> %</strong></li>
    <li>Suffrages exprimés (hors blancs) : <strong><%= suffragesExprimes %></strong></li>
  </ul>

  <ul class="classement">
    <% rows.forEach(function(r, i) { %>
      <li class="classement-item">
        <span class="classement-rang">#<%= i + 1 %></span>
        <% if (r.photoUrl) { %><img src="<%= r.photoUrl %>" alt="" class="classement-photo"><% } %>
        <span class="classement-nom"><%= r.prenom %> <%= r.nom %></span>
        <span class="classement-score"><%= r.voix %> voix <span class="classement-pourcentage">(<%= r.pourcentage %> %)</span></span>
      </li>
    <% }); %>
    <li class="classement-item classement-item-blanc">
      <span class="classement-rang"></span>
      <span class="classement-nom">Votes blancs</span>
      <span class="classement-score"><%= blancs %> voix <span class="classement-pourcentage">(<%= pourcentageBlancs %> %)</span></span>
    </li>
  </ul>
</div>

<%- include('_foot') %>
